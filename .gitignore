node_modules/
.DS_Store
.claude/
dist/

# Per-user data — these are generated at runtime and should not be committed.
# The curated baseline (data/golden.json) IS committed so a fresh clone works
# out of the box; remove the negation if you'd rather not ship it.
data/custom-probes.json
reports/
!data/golden.json

# Editor / OS noise
.vscode/
.idea/
*.swp
*.swo
Thumbs.db
